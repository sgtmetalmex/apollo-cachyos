# makepkg / build artifacts — recreated on every `makepkg` run
src/
pkg/
*.pkg.tar.zst
*.pkg.tar.xz
*.log
*.orig
*.rej

# IDE / editor cruft
.vscode/
.idea/
*.swp
.DS_Store

# AI tooling — intentionally NOT shipped with this repo (see README "AI-assisted
# disclosure"). The AI tooling itself is generic and lives outside the project.
.claude/
.cursor/
graph/
graph-corpus/
graphify-out
GRAPH_LOCATION.md
*future*prompt*
